# hooks/

**Cross-feature** React hooks only (used by 2+ features or the app shell), e.g.
`use-mobile`, `use-debounce`, `use-media-query`.

Data-fetching hooks are NOT here — those live with their feature in
`features/<feature>/hooks.ts`.
