/**
 * App-wide static config. Single source of truth for branding/metadata.
 */
export const siteConfig = {
  name: "MillionDocs",
  tagline: "Export OS",
  description:
    "Run export operations from one workspace. MillionDocs centralises buyers, products and shipments, then auto-generates every export document.",
} as const;
